{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main TFT Team Helper window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
